# 2026-08-27 工作日志

## 项目结构盘点（用户要求介绍各部分 + 指出不合理处）
- 结论：项目实际是 Python v2.0（FastAPI + LangGraph，推荐/风控/授信/退款/履约 5 场景），而根目录 README.md / plan.md 描述的是 v1 旧设计（三语言 python/java/go、supervisor.py、4 个旧 Agent、docs 下 4 个面试 md），与代码严重脱节。
- 关键发现：
  - java/、go/ 目录不存在；supervisor.py、user_profile_agent.py、kg_user_profile_agent.py、inventory_agent.py、supply_chain_agent.py 均不存在（已被 services/ 下确定性 Service 重构替代，见 python/docs/multi-agent.html「Agent → Service 迁移」）。
  - docs/prd-architecture/ 与 interview-prep/ 仅残留 echarts.min.js / mermaid.min.js 库文件，HTML 主文档在 .trae-html-share-packages/*.zip 内。
  - 实际代码结构：agents(3 个 LLM Agent: product_rec/marketing_copy/meta) + orchestrator(graph/dynamic_engine) + services(24 个确定性 Service/工具) + llm(factory/router) + database + tests(19 个测试)。
  - docker-compose.yml 的 api 硬依赖 vllm（需 NVIDIA GPU），无 GPU 环境一键 up 会失败。
- 已向用户提交删除建议清单，等待用户确认后执行。

## README 重写（v2.0 对齐）+ 第二轮复查
- 用户确认后重写了根目录 README.md，对齐 v2.0 实际状态：3 LLM Agent + 7 确定性 Service、LangGraph 双引擎、5 大场景、16 个真实 API、真实目录树、可靠性设计（四层/三层防护、Saga、安全网关、追踪、多模型路由）、测试状态（142/143）、已知问题表。
- README 中所有引用的 python/ 路径已逐一验证存在；测试文件 19 个（143 用例）与文档一致。
- 第二轮新发现（已写入 README「已知问题」表）：
  1. test_recall_returns_products_without_profile 失败：_recall 排序键含 random.random()，分数/库存相同时随机打乱致 P001 被挤出前10（断言不稳定，未修复）。
  2. ECOM_PROFILE_SOURCE 死配置：settings.profile_source 无代码引用，docker-compose 中该变量误导，可移除。
  3. settings 默认 guardrails_jwt_enabled=True 且 jwt_secret=None → 不配 .env 直接启动 fail-fast；.env.example 默认关闭 JWT。
  4. LICENSE 文件缺失（README 声明 MIT）；git remote 为 cch15525863911-lab，README 旧链接 bcefghj 已失效。
  5. docker-compose api 硬依赖 vllm（GPU），无 GPU 需用「仅核心依赖」方式或注释 vllm。
- 测试用 python/.venv 运行：ensurepip 补装 pip 后安装 pytest+pytest-asyncio；托管 python 环境未使用（项目 venv 已有依赖）。

## 三项修复（召回 bug / JWT 冲突 / LICENSE）— 全部完成，测试 143/143 全绿
1. 召回 bug：`agents/product_rec_agent.py` `_recall` 排序用 `random.random()` 作 tie-breaker 导致结果不可复现、测试不稳定 → 改为基于 product_id 的确定性 hash 打散（`hashlib.md5`），并移除 `import random`；`tests/test_recall.py` 断言从"P001 必须在"修正为"fallback 候选均来自 MOCK_PRODUCTS"（语义正确）。
2. JWT 冲突：统一为"鉴权默认开启(fail-closed) + 模板提供 dev- 占位密钥 + 启动强校验"。
   - `python/.env.example`：`ECOM_GUARDRAILS_JWT_ENABLED=true`、`ECOM_JWT_SECRET=dev-only-insecure-...`(55字符)、新增 `ECOM_DEBUG=true`（本地开发默认，生产必须 false+强密钥）。
   - `python/main.py` lifespan：校验密钥 ≥32 字符 + 非 debug 禁用 dev- 前缀，fail-fast。
   - `python/services/guardrails.py`：去掉 `getattr(settings,'jwt_secret','dev-secret...')` 硬编码 fallback（消除"未配置静默用公开密钥"隐患），verify_token 对 None 密钥 fail-closed。
   - 新增 `python/conftest.py`（全局测试 JWT 密钥，setdefault 不覆盖外部值）；`tests/test_neo4j_startup.py` 测试密钥改为 ≥32 字符。
3. LICENSE：新增根目录 `LICENSE`（MIT 标准模板，copyright 用 git config user.name: cch15525863911-lab）。
4. README 同步更新：测试 143/143 全绿、已知问题表（3 项已修）、快速上手 JWT 说明。
5. 遗留待办（用户未要求）：ECOM_PROFILE_SOURCE 死配置、docker-compose vllm GPU 硬依赖、docs/interview-prep 残缺目录 + .trae-html-share-packages、.testenv 空壳。
