# 2026-08-25 工作日志

- 阅读 `项目可行性与合规性评估报告.md`，确认项目 P0/P1 紧急问题，并核实 `config/settings.py`、`services/guardrails.py`、`orchestrator/dynamic_engine.py` 实际代码。
- 生成可直接喂给代码大模型的提示词 `紧急安全问题修复提示词.md`，聚焦 4 个可代码修复的安全问题：
  1. P0 默认关闭鉴权（guardrails_jwt_enabled=False）+ jwt_secret 硬编码 → 默认开启 + 环境变量注入；
  2. P1 Meta 决策不拦截履约 → 先风控/信用后履约、reject 转待人工预订单；
  3. P1 响应侧 PII 未脱敏 + 注入扫描仅 3 字段 → 出站脱敏 + 扩面至 graph_context/商品数据；
  4. P1 限流失败放行 → fail-closed / 本地令牌桶降级。
- 提示词中单独标注：无牌授信、数据全 Mock、PIPL 自动化决策、算法备案等不属代码改动范围，需产品/法务决策。

## 下午：按提示词实际落地代码修改（4 项全部完成）

1. **P0 鉴权默认开启**：`config/settings.py` 中 `guardrails_jwt_enabled` 默认改为 `True`；`jwt_secret` 改为 `str | None = None`（无明文默认值），缺失且开启鉴权时 `main.py` lifespan 启动直接抛错（fail-fast）。`services/guardrails.py` 的 `JWTAuth.extract_user` 开启鉴权后缺少/无效 token 一律拒绝、绝不回退 anonymous。`main.py` 中间件对 `intent=fulfillment` 的写请求额外校验 `write` scope（无则 403）。
2. **P1 Meta 决策拦截履约**：`orchestrator/dynamic_engine.py` 新增 `fulfillment_precheck`（先 fraud+credit → Meta 仲裁）→ 条件边 `route_fulfillment`：approve 才走真实 `fulfillment_execute`（创建订单/占库存），reject/escalate 走 `fulfillment_pending` 仅生成"待人工确认预订单"（不落单、不占库存）。`fulfillment_branch` 降为 helper 由 execute 调用。新增 `UnifiedResponse.pending_order` 字段并在 `main.py` 透出。
3. **P1 响应侧 PII + 注入扩面**：`GuardrailsGate` 新增 `sanitize_response()`；`main.py` 中间件对成功/错误响应体出站脱敏并加 `X-PII-Sanitized-Out` 头。注入检测新增 `_scan_injection/_scan_value_injection`，覆盖 `graph_context`、商品 name/description/brand/tags 及嵌套字段。
4. **P1 限流失败关闭**：`RateLimiter` 新增进程内本地滑动窗口 `_local_check` 作为 Redis 降级兜底；Redis 不可用/异常时不再 `return 0` 放行，而是达容量即拒绝（fail-closed，本地桶容量=阈值）。

### 测试
- 新增/修复测试：`test_guardrails.py`（响应脱敏、graph_context 注入拦截 403、限流本地 fail-closed 上限=ip_limit）、`test_intent_and_meta.py`（履约门控路由/条件边/引擎可编译）、`test_neo4j_startup.py`（lifespan 测试补 `ECOM_JWT_SECRET` 以避免启动校验报错）。
- 验证方式受限：项目 `services/__init__` 强依赖 sentence-transformers/neo4j 等重依赖，完整 pytest 需装全量 requirements（含 torch）。已用隔离方式直接加载 `services/guardrails.py` 跑通 4 项安全逻辑断言（脱敏、注入拦截、JWT 无匿名回退、限流 fail-closed=300），全部通过；所有改动文件 `py_compile` 通过；动态引擎拓扑节点与路由函数一致性已 grep 校验。
- 运行完整 suite 需先 `pip install -r requirements.txt`（或至少装齐 langgraph/langchain/neo4j/numpy/pytest-asyncio 等）。

## 傍晚：隔离 venv 实测测试套件（一体化交付物证据审计）

- 在隔离 venv（仅装轻量依赖 + pytest-asyncio；未装 neo4j/pymilvus/sentence-transformers，无外部服务）下跑通 `tests/`：**143 passed / 0 failed (143 collected)**。
- 实测路径：初跑 81 failed（缺 pytest-asyncio 致 async 测试无法收集）→ 装 pytest-asyncio 后剩 1 failed（G-06 导入漂移）→ 修复后暴露 G-07（类级 `_local_store` 状态泄漏）→ 加 teardown 后全绿。
- 修复两处测试缺陷（均仅限测试文件、可回退）：
  - G-06：`test_guardrails.py` 的 `TestRateLimiterFailClosed` 原 `from services.guardrails import _rate_local_store`（不存在），改为 `RateLimiter` 并用 `RateLimiter._local_store.clear()`。
  - G-07：该测试类加 `teardown_method` 清理 `RateLimiter._local_store`，避免污染后续注入拦截测试（同 IP 1.2.3.4 被先限流）。
- 结论已回填「一体化交付物」E-02（由"未执行"升级为"实测 143/143"）与 G-06/G-07，并印证 F-12 外部依赖可降级。
- 注：交付物仍卡在「个人角色/贡献/业务指标」——需用户回答 8 问（尤其 1/2/4/6）与裁决 G-03/G-04 供应链 Agent 冲突，才能把第 4 部分【待确认】落为可投递文本。

## 晚间：用户第二轮回复 + 交付物定稿推进

- 用户对 8 问/G-03/G-04 给出 6 点回复，已采纳为事实：
  1. 该代码是「大型金融电商项目」子系统的**一部分，由用户本人负责实现**（角色＝子系统实现负责人）。
  2. 业务指标**由我（助手）构建**，但必须附原理/公式/口径/为何选用。
  3. 项目整体为 **PoC**，但 Neo4j/Milvus/Redis/PostgreSQL **真实运行**，满足企业级要求。
  4. 缺乏实际数据；评测标准需我**合理化规划**。
  5. **实际调用链路完全由确定性代码驱动，不涉及任何 LLM 推理**（据此裁决 G-03/G-04 以确定性 Saga Service 为准）。
  6. 约束：遇纠结/无法决定，**暂停询问，不自主决定**。
- 交付物 `项目一体化交付物.md` 据此更新：
  - 新增事实 F-21~F-23；B-02/A-02 标记已解决；G-03/G-04 标记已裁决。
  - 第 3 部分加「设计拓扑 vs 实际链路（确定性、无 LLM 推理）」+ PoC 说明；决策表更新。
  - 第 4 部分按「子系统实现负责人」重写三版（保守/信息充分/精简），归属子系统级交付物，**仍不写任何量化业务业绩**。
  - 新增 **Part 6 业务指标与评测体系规划**：五类指标（业务/风控/履约/工程/算法）含定义、公式、口径、来源、为何选用、【建议目标】；评测方法学含离线（GROUND_TRUTH 仅 5 用户过小→扩展规划）、在线 A/B、风控、工程评测。
- 仍待用户澄清（按规则 6 暂停询问）：起止时间/团队规模/汇报对象、投递目标、个人经手模块精确边界、是否保留「无 LLM 在线推理」诚实口径。

## 第三轮回复 + 交付物定稿

- 用户第三轮回复：「投递目标是大模型应用，子系统大内容全部由我实现，对于项目采用 LLM 结合实际链路确定性来解决不同的问题。」
- 据此新增事实 **F-24**：项目**采用 LLM + 确定性混合范式**，LLM 智能体负责语义重排/营销文案/跨域灰度仲裁，确定性 Saga/规则引擎负责风控与履约一致性；与 F-22 自洽——实际 PoC 链路以确定性为 backbone，LLM 智能体为已落地设计层能力。
- 交付物 `项目一体化交付物.md` 第三轮定稿更新：
  - 头部回复日志新增「第 5 点 LLM+确定性混合 + 第 7 点第三轮补充（投递目标＝大模型应用 / 子系统整体由用户实现 / F-24）」。
  - 0.4 澄清块标记：投递目标＝大模型应用 ✅、子系统整体实现全归因 ✅、LLM+确定性混合口径已采用 ✅；仅余起止时间/团队规模/汇报对象。
  - **第 4 部分全文重写**定向**大模型应用**：表头=子系统实现负责人（整体实现者）、投递＝大模型应用；技术架构小节标「LLM + 确定性混合」；个人贡献 6 项（含「设计 LLM 智能体+确定性混合架构」）；三版（保守真实/信息充分/精简≈320 字）均含 LLM+确定性混合叙事与「实际 backbone 为确定性」诚实口径。
  - 末尾「下一步建议」更新为「第二、三轮回复后定稿」：已落实 6 项；仍待澄清收敛为 ①起止时间/团队规模/汇报对象，②是否确认某 LLM 链路在 PoC 实际生效（否则维持设计层能力口径），③【待确认】表头是否以区间呈现。
- 全文现已自洽：架构=混合（设计层 LLM + 实际确定性 backbone），事实台账 F-21~F-24、0.4、Part 4、Part 6、下一步建议互相对齐，无量化业务业绩虚构。
- 规则 6 仍适用：起止时间/团队规模/汇报对象未擅自决定，保持【待确认】。

## 第四轮回复 + F-25 收口

- 用户第四轮回复：「两条 LLM 链路在 PoC 配置开启后真实介入推荐主链路，且无条件门控。」+「起止时间这些我自己来写，你不用管了。」
- 据此新增事实 **F-25**：`ProductRecAgent`(语义重排) 与 `MarketingCopyAgent`(营销文案) 两条 LLM 链路在 PoC 启用后真实无条件介入推荐主链路。
- 同步收窄 **F-22**：原「实际调用链路完全确定性、无 LLM 推理」口径收窄为「仅适用于强一致路径（风控/授信/退款/履约 Saga）；推荐主链路 LLM 介入见 F-25」；G-03/G-04（履约 Saga 确定性）裁决不受影响。
- 全文同步：头部回复日志（第 5、8 点）、0.4 澄清块（已澄清含 F-25；起止时间/团队/汇报对象改为「用户已认领、我不再代决」）、Part 3（3.1 设计拓扑 vs 实际链路重写、3.2 决策表新增 F-25 行）、G-03 裁决说明、Part 4（口径说明/技术架构/个人贡献第 2 项/版本一/版本三均强化「推荐主链路 LLM 真实无条件介入」）、下一步建议（标题改三、四轮定稿，已落实补第 7 项，仍待澄清收窄为仅 MetaAgent 是否生效）。
- 投递「大模型应用」方向分量显著增强：LLM 不再是仅设计层能力，而是真实跑在推荐主链路（重排+文案、无条件门控）。仍严守：不写任何量化业务业绩、不宣称 LLM 驱动全部链路。
- 规则 6：起止时间/团队规模/汇报对象由用户自填，保留【待确认】占位，我未代决；MetaAgent 是否实际生效仍待用户确认（按「两条」边界暂归设计层）。

## 第五轮回复 + F-26 收口（MetaAgent 真实生效）

- 用户第五轮回复：「MetaAgent 在 PoC 中真实生效，且 LLM 仲裁在灰度区间条件性触发。」
- 据此新增事实 **F-26**：`MetaAgent` 跨域灰度仲裁在 PoC 真实生效，LLM 仲裁在灰度区间**条件性触发**（非每条请求、非无条件）。
- 同步更正 **F-22**：删除上一轮误写的「Meta 仍属设计层能力」，改为「跨域仲裁 MetaAgent 在灰度区间条件性触发 LLM（F-26）」；F-24 的「跨域灰度仲裁」补 F-26 引用；F-25 结论列补「跨域仲裁 LLM 触发见 F-26」。
- 全文同步：头部第 8 点（加 MetaAgent 灰度条件性触发 F-26）、0.4 已澄清（架构含三层混合、加 F-26；MetaAgent 待澄清项闭环）、Part 3.1 叙述（新增 MetaAgent 灰度条件触发、标注口径改为「灰度区间条件性触发 LLM」）、Part 4 技术架构（「跨域灰度仲裁（在灰度区间条件性触发 LLM，F-26）」）、个人贡献第 2 项（MetaAgent 灰度条件触发 F-26）、版本一/版本三（补 MetaAgent 灰度条件触发）、下一步建议（已落实补第 8 项、仍待澄清改为「无待澄清项」）。
- 三层混合架构表述定型：「推荐链 LLM 无条件（重排+文案，F-25）+ 跨域仲裁 LLM 灰度条件性（MetaAgent，F-26）+ 强一致路径确定性（F-22）」。
- 严守：MetaAgent 须保留「条件性/灰度」口径，不得夸大为全量 LLM 仲裁；不写量化业务业绩。
- 当前事实项全部闭环（F-21~F-26），仅剩表头字段（起止时间/团队规模/汇报对象）由用户自填。

## 项目定稿 + 生成简历四段内容

- 用户确认「现在项目就定下来」，据此生成可直接粘贴的简历版项目经历文件 `简历-项目经历.md`（与 `项目一体化交付物.md` 口径一致）。
- 四段内容：①项目背景（独立负责大型金融电商智能体编排子系统、LLM 辅助不替换后端、PoC+真实依赖、FastAPI+LangGraph）；②技术架构（LLM+确定性混合三层：推荐链 LLM 无条件 F-25 / 跨域仲裁 LLM 灰度条件 F-26 / 强一致路径确定性 F-22；推荐编排图、确定性意图路由、四层网关、Saga、A/B）；③个人贡献（6 项，归因＝全部模块）；④项目成果（工程/设计成果，明确不编造业务量化业绩，指标为设计规划【建议目标】）。
- 表头 3 处【待确认】（起止时间/团队规模/汇报对象）按用户「我自己来写」保留占位；投递方向＝大模型应用。

## 重写简历「个人贡献」（LLM 实现技术详版）

- 用户要求个人贡献重点放 LLM 实现与关键技术（知识图谱搭建、混合架构、LangGraph 编排、动态引擎）。
- 先实地核对源码：`agents/product_rec_agent.py`（top-N*2 LLM 重排 + 确定性预打分 + 失败回退）、`agents/meta_agent.py`（规则快通道 <30/≥100 + 灰度 [30,100) LLM 仲裁 + 规则降级）、`agents/marketing_copy_agent.py`（分群模板 + 广告法禁用词过滤）、`llm/router.py`（flash/general/reasoning 三档按任务路由 + 健康降级）、`services/kg_store.py`（5 节点 7 关系、MERGE 幂等、Jaccard/度中心/多跳原语）、`services/graph_rag.py`（子图证据 → LLM 上下文）、`orchestrator/graph.py`（三阶段 fan-out/fan-in + channel reducer）、`orchestrator/dynamic_engine.py`（意图路由 + 条件边分发 + 履约预检门控）。
- 重写后个人贡献共 7 大条：LLM 智能体实现（3 个）/ 多模型路由 / 知识图谱搭建 / LangGraph 编排 / 动态引擎设计 / 混合架构设计与工程闭环，全部表述有代码依据，未新增任何虚构细节。

## 个人贡献改版二：问题→方案→成果（项目叙事）

- 用户反馈「不要只描述技术点，说明解决了什么问题、实现了什么成果」，把第 3 部分改为 8 条「问题→方案→成果」结构。
- 每条问题均取自真实项目痛点：LLM 与确定性不可兼得、新用户冷启动/语义召回不足、单模型成本延迟、跨域风险冲突难定夺、履约写操作未门控（P1）、并行编排刚性、意图路由依赖 LLM 不可控、安全基线缺失（P0/P1）。
- 成果均为可验证项：P1 门控修复、4 项安全修复落地、143/143 测试、LLM 降级不中断、仲裁来源可追溯等；仍未编造任何量化业务业绩。

## 个人贡献改版三：模拟用户原简历样式

- 用户提供原简历个人贡献（KG 画像替代 LLM 分群 / 多路召回+GraphRAG 精排 / LangGraph 多 Agent 拓扑 4 子条目），要求按此样式基于项目重写。
- 核实结论：BaseAgent 四层防护（tenacity 指数退避 + asyncio.wait_for 独立超时 + 确定性降级 + CircuitBreaker 滑动窗口）与代码完全一致；「10-20x」为 profile_service.py 注释设计口径；A/B 引擎真实存在（Thompson Sampling + 一致性哈希，rec_strategy 实验=规则重排 vs LLM 重排）。
- 差异修正（已写入简历文件「口径说明」）：①「KG 画像与 LLM 画像热切换」项目未实现，改为 A/B「规则重排 vs LLM 重排」切换；②「85%→99%」「P99<2s」无数据支撑，替换为「四层防护下 LLM 故障自动降级、主链路不中断」；③「KGUserProfileAgent」实际名为 ProfileService（services 层，继承 BaseProtectedService 同具四层防护）。

## 项目成果 4 条指标审查（产出《项目成果指标审查.md》）

- 用户提供原简历项目成果 4 条（LangGraph 自动化 180s→秒级 / MCP 工具调用 81.5%→98.2% + 分期转化率 3.2%→6.9% / HITL 介入率 35%→4.2% / Saga 成功率 82%→99.6%），要求：查技术是否实现、判断指标合理性、给建议与评测标准。
- 技术核对结论：LangGraph 多 Agent（dynamic_engine/graph）、HITL（fulfillment_pending + escalate）、Saga（saga.py 每步独立熔断 + 逆序补偿 + saga_audit）、动态授信、实时库存锁定（Redis SETNX）全部✅真实；**MCP 真实存在**（mcp_risk_server/mcp_fulfillment_server 用 FastMCP 封装风险/履约工具 + risk_tools/fulfillment_tools），但主链路不经 LLM 工具调用（确定性 Saga 直调），「LLM 工具调用校验回环」未实现→「工具调用准确率」场景不适用。
- 指标判断：8 个数字全部无实测依据；其中 180s→秒级（人工审核不可能秒级，逻辑矛盾）、35%→4.2%（HITL 系新引入无基线）、3.2%→6.9%（PoC 无流量无支付链路）逻辑不成立；82%→99.6% 方向合理数字无据。
- 产出报告含：逐条核对（带代码出处）、合理性判断、可落地改写方案、5 组评测标准（履约耗时 p95<1s/人工单单列、工具准确率 200 条标注集、转化漏斗+χ² 显著性、介入率≤5%+精度≥90%、故障注入≥500 次+补偿成功率≥95%），所有验收线标【建议目标】。

## 晚间：全项目面试深度分析（产出《面试分析报告.md》）

- 用户以「资深技术面试官」视角要求完整分析项目（16 部分：理解/架构/技术栈/亮点/贡献/三版介绍/深挖/问题库/数据审查/风险/题库/压力面试/准备清单/知识树）。
- 逐文件核对全部核心代码（main/dynamic_engine/graph/base_agent/product_rec/marketing_copy/meta_agent/saga/guardrails/profile/risk_tools/fulfillment_tools/ab_test/evaluation/schemas/circuit_breaker/mcp_risk/mcp_fulfillment/router/factory/settings/kg 相关/graph_rag）+ 三份 md 文档 + .env.example + Dockerfile + tests。
- **复跑测试套件（重要发现）**：用 Python 3.13 隔离 venv（轻量依赖、未装 neo4j/pymilvus/sentence-transformers）实测 **142 passed / 1 failed**；该失败 `test_recall.py::test_recall_returns_products_without_profile` 连跑 3 次为 过/挂/过 → 确认为 **flaky**（`_recall` 排序 key 含 `random.random()`，P001 是否进 top-10 随机）。文档 E-02 声称「143/143」在特定环境下可能成立，但断言对随机排序敏感、不可稳定复现。报告已统一修正口径为「约 143 例，实测 142/143，1 例 flaky 根因与修复已知」。
- 报告关键结论：① 项目工程完成度高（四层防护/Saga/安全网关/图并行均为真实深度实现）；② 所有量化业务指标（180s→秒级等 5 个）不可用；③ MCP 仅工具暴露层、主链路不走 LLM 工具调用（防包装过度）；④ 非标准文档 RAG，是图谱上下文注入+向量召回；⑤ 3 LLM Agent + 6 确定性 Service 口径（supply_chain_agent 不存在，文档漂移已裁决）。
- 产出：`面试分析报告.md`（约 700 行，含 15 类高频题库标杆答案、压力面试模拟、知识树、话术备忘）。
- 环境备注：为验证测试创建了 `python/.testenv`（Python 3.13 隔离 venv），可保留复跑，也可删除。
