# 2026-08-26 工作日志

## 建立《项目技术档案》(docs/项目技术档案.md)
- 全面通读 python/ 仓库代码(main.py、orchestrator、agents、llm、models、services×25、database、tests×19、docs),产出 15 章完整技术档案。
- 关键事实核对结论:
  - 3 个 LLM Agent(ProductRec/MarketingCopy/Meta)+ 6 个确定性 Service(Profile/Fraud/Credit/Refund/Inventory/Fulfillment),混合架构。
  - LangGraph 双图:推荐三阶段图(fan-out/fan-in + reducer)+ 动态引擎(意图路由→5 分支→Meta→条件门控)。
  - 履约写操作有门控:approve 才走真实 Saga,否则生成待人工预订单(不落单不占库存)。
  - 主链路不经 LLM 工具调用;MCP 服务器×2 只是工具暴露层;langchain-mcp-adapters 引入但未用。
  - 数据设施:PG(订单/仓库/库存)、Redis(限流/预占锁/CF/特征)、Milvus(商品向量 HNSW 512d)、Neo4j(图谱+画像特征+多跳召回)。
  - 四层防护(重试/超时/降级/熔断)贯穿 Agent 与 Service;Saga 4 步+逆序补偿+独立熔断器。
- 风险点记录(面试使用):8 个量化指标无实测(180s→秒级、81.5→98.2 等);风控规则用 random 模拟特征不可复现;工作流引擎是 Temporal 风格骨架(内存存储);A/B 主链路用一致性哈希而非 Thompson;tests 顶层 73 个 test 函数(简历称 143/143 需复跑核实口径);.venv 未装 pytest。
- 已有资料:docs/简历-项目经历.md、docs/项目成果指标审查.md 为上一轮产出,本次档案已纳入其口径。
